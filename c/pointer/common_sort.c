#include <stdio.h>

int main(void)
{
	
	return 0;
}

void sort(void *s, int n, int size, int (*cmp)(const void *, const void *))
{
	int i, j;

	for(i = 0; i < n - 1; i++)
	{
		for(j = 0; j < n - 1 - i; j++)
		{
			if(cmp())
		}
	}
}
